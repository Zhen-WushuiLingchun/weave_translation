# Security and privacy

## Data flow

织语只在用户主动翻译，或用户为当前站点明确启用自动翻译时，向所配置的模型端点发送经过过滤的文本。整页模式会发送页面摘要样本以及逐批文本；划词模式发送选中文本、所在段落和邻近段落；字幕模式发送视频标题、字幕样本和当前时间附近的字幕句。术语库只发送本地检索命中的词条，模型无法直接读取完整词典。

无字幕视频必须由用户点击启动。扩展随后请求可选 `tabCapture` 权限，将当前标签页正在播放的音频切成内存 WAV 片段并发送到用户配置的 Audio Transcriptions 服务。扩展不下载完整视频、不识别尚未播放的未来音频，也不持久保存原始音频或 ASR 文本。

密码框、表单控件、可编辑区域、代码块、隐藏节点和扩展自身节点会在内容脚本中排除。

## Native PDF side panel

原生 Chrome PDF 查看器保持不变。`contextMenus` 仅在用户点击织语菜单后交付选中文字；待交付选文暂存在可信 `storage.session`，读取后即移除，不自动重放。`sidePanel` 用于显示翻译 UI；`activeTab` 用于用户主动截取当前标签页。截图只在侧边栏本地预览/圈选，明确确认发送后才进入模型请求。

PDF.js 在本地按需解析在线文档（保留原站点访问权限）或用户选定的本地文件。单文件限制 100 MiB；不会运行 PDF 自带 JavaScript，不上传原始文件。上下文默认 8k 输入估算预算，可选 16k；只发送选文、有限段落、术语命中和按需图片。无法定位页码时明确告诉模型页码未知，不编造邻近上下文。模型输出通过既有受限 Markdown/LaTeX 渲染器处理。

PDF 图片/结果缓存独立于网页缓存，默认内存会话模式。可选磁盘模式保存到 IndexedDB，期限为 1/7/30 天，容量为 32/64/128 MiB（磁盘所有 PDF 条目合计）；启动/读写时清理到期数据和超额 LRU 条目，浏览器关闭期间不保证即时擦除。未解析、没有内容指纹的文档截图仅保存在会话中。缓存不含原 PDF、API Key 或原始请求；缓存值可能含敏感截图和译文，未加密。提供按文档和全量清除；停用磁盘模式时清除 PDF 磁盘缓存。不创建服务商 Files API 文件；本地清除不影响服务商按其政策保存的请求。

## API keys

- 每个服务连接使用独立 `secretRef`
- 持久模式：`chrome.storage.local` 中的隔离密钥映射
- 会话模式：`chrome.storage.session` 中的隔离密钥映射
- 两者均设置为 `TRUSTED_CONTEXTS`
- 内容脚本只获得 `hasApiKey` 布尔状态，不接收密钥正文
- 日志、错误信息、缓存键和导出不包含密钥

Chrome 扩展存储不是操作系统级加密保险箱。共享操作系统账户或高风险设备上应使用会话模式。

## Reporting

公开发布前会添加专用安全联系方式。目前请在本地仓库记录问题，不要在报告中附带真实 API Key、完整私密网页文本或浏览历史。
