# Security and privacy

## Data flow

织语只在用户主动翻译，或用户为当前站点明确启用自动翻译时，向所配置的模型端点发送经过过滤的文本。整页模式会发送页面摘要样本以及逐批文本；划词模式发送选中文本、所在段落和邻近段落；字幕模式发送视频标题、字幕样本和当前时间附近的字幕句。

密码框、表单控件、可编辑区域、代码块、隐藏节点和扩展自身节点会在内容脚本中排除。

## API keys

- 持久模式：`chrome.storage.local`
- 会话模式：`chrome.storage.session`
- 两者均设置为 `TRUSTED_CONTEXTS`
- 内容脚本只获得 `hasApiKey` 布尔状态，不接收密钥正文
- 日志、错误信息、缓存键和导出不包含密钥

Chrome 扩展存储不是操作系统级加密保险箱。共享操作系统账户或高风险设备上应使用会话模式。

## Reporting

公开发布前会添加专用安全联系方式。目前请在本地仓库记录问题，不要在报告中附带真实 API Key、完整私密网页文本或浏览历史。
